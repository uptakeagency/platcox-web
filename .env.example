# Cloudflare Turnstile site anahtarı (public). Lokal build için resmi test anahtarı:
PUBLIC_TURNSTILE_SITE_KEY=1x00000000000000000000AA

# Aşağıdaki dört değişken sunucu tarafı gizli değerlerdir; .env'de DEĞİL,
# Cloudflare Pages projesinde tanımlanır (Settings → Environment variables):
# RESEND_API_KEY=
# TURNSTILE_SECRET_KEY=
# CONTACT_TO=
# CONTACT_FROM=
